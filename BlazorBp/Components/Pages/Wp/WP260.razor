@code {
  /// <summary>Holt oder setzt das Model.</summary>
  [Parameter]
  public WP260Model? WP260Model { get; set; }

  /// <summary>Holt oder setzt die Id im modalen Dialogs.</summary>
  [Parameter]
  public string? ModalId { get; set; }

  /// <summary>Holt oder setzt die Art des modalen Dialogs (Neu, Bearbeiten, Löschen).</summary>
  [Parameter]
  public string? ModalArt { get; set; }

  /// <summary>Betroffener EditContext.</summary>
  [Parameter]
  public EditContext? ModalEditContext { get; set; }

  /// <summary>Betroffener ValidationMessageStore.</summary>
  [Parameter]
  public ValidationMessageStore? ModalMessages { get; set; }
}

@if (string.IsNullOrEmpty(ModalId))
{
  <EditForm Enhance method="post" EditContext="ModalEditContext" FormName="wp260modal"></EditForm>
}
else
{
<div class="modal xxxfade" id="idmodal" tabindex="-1" role="dialog" aria-labelledby="lbldetails" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="lbldetails">Anlage @Functions.Iif(ModalArt == "Table_Edit", "ändern", Functions.Iif(ModalArt == "Table_Delete", "löschen", Functions.Iif(ModalArt == "Table_Copy", "kopieren", "erfassen")))</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Schließen"></button>
      </div>
      <div class="modal-body">
        <EditForm Enhance method="post" EditContext="ModalEditContext" OnValidSubmit="ModalSubmit" FormName="wp260modal">
          @* <DataAnnotationsValidator/> *@
          <ValidationSummary class="text-danger"/>
          <InputText type="hidden" @bind-Value="WP260Model!.Nr"/>
          <InputText type="hidden" @bind-Value="WP260Model!.ReadonlyHiddenError"/>
          <input type="hidden" name="SubmitControl"/>
          <div class="row mt-1 g-0">
            <LabelInputValid AutoPostback="" For="@(() => WP260Model!.Nummer)" VerticalColClass="form-group col-md-2"/>
            <LabelInputValid AutoPostback="" For="@(() => WP260Model!.Bezeichnung)" VerticalColClass="form-group col ms-1"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid type="combobox" AutoPostback="" For="@(() => WP260Model!.Wertpapier)" DataList="WP260Model!.AuswahlWertpapier" VerticalColClass="form-group col-md-10"/>
            <SubmitButton class="btn btn-secondary col ms-1" For="@(() => WP260Model!.Wpdetails)"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid type="combobox" AutoPostback="" For="@(() => WP260Model!.Status)" DataList="WP260Model!.AuswahlStatus" VerticalColClass="form-group col ms-1"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid type="combobox" AutoPostback="" For="@(() => WP260Model!.Depot)" DataList="WP260Model!.AuswahlDepot" VerticalColClass="form-group col"/>
            <LabelInputValid type="combobox" AutoPostback="" For="@(() => WP260Model!.Abrechnung)" DataList="WP260Model!.AuswahlAbrechnung" VerticalColClass="form-group col-md-4"/>
            <LabelInputValid type="combobox" AutoPostback="" For="@(() => WP260Model!.Ertrag)" DataList="WP260Model!.AuswahlErtrag" VerticalColClass="form-group col-md-4 ms-1"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid rows="2" AutoPostback="" For="@(() => WP260Model!.Notiz)" VerticalColClass="form-group col"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid rows="8" AutoPostback="" For="@(() => WP260Model!.Data)" VerticalColClass="form-group col"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid AutoPostback="" For="@(() => WP260Model!.Angelegt)" VerticalColClass="form-group col-md-6"/>
            <LabelInputValid AutoPostback="" For="@(() => WP260Model!.Geaendert)" VerticalColClass="form-group col ms-1"/>
          </div>
          <div class="row mt-1 g-0">
            <LabelInputValid AutoPostback="" For="@(() => WP260Model!.Valuta)" VerticalColClass="form-group col-md-6"/>
            <LabelInputValid AutoPostback="" For="@(() => WP260Model!.Stand)" VerticalColClass="form-group col ms-1"/>
          </div>
          <div class="row mt-1 g-0">
            <SubmitButton class="btn btn-primary col-md-2 ms-1" For="@(() => WP260Model!.Ok)"/>
            <SubmitButton class="btn btn-secondary col-md-2 ms-1" For="@(() => WP260Model!.Abbrechen)"/>
          </div>
        </EditForm>
      </div>
    </div>
  </div>
</div>
}

@code {
  /// <summary>Initialisierung der Komponente.</summary>
  protected override void OnInitialized()
  {
  }
 
  /// <summary>Submit-Methode für das modale Formular wird fast nie aufgerufen.</summary>
 public void ModalSubmit()
  {
    if (WP260Model != null)
    {
      Functions.MachNichts();
    }
  }
}
